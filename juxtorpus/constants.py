""" String Constants

These strings are used to maintain consistent pandas column names.
"""

# format this with column name and corpus numeric id.
# e.g. CORPUS_ID_COL_NAME_FORMAT.format(col_name, corpus_idx)
CORPUS_ID_COL_NAME_FORMAT = "{}_corpus_{}"
